name: tests

on:
  push:
    branches:
      - m*

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Extract branch name
        run: echo "BRANCH=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: ~1.22.5

      - name: Check out code
        uses: actions/checkout@v3

      - name: Linters
        uses: golangci/golangci-lint-action@v3
        with:
          version: v1.53.3
          working-directory: .

  tests:
    runs-on: ubuntu-latest
        
    steps:
      - name: Extract branch name
        run: echo "BRANCH=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV

      - name: Set up Go
        uses: actions/setup-go@v3
        with:
          go-version: ^1.22.5

      - name: Check out code
        uses: actions/checkout@v3

      - name: Start services with Docker Compose
        run: docker-compose up -d --build --file docker-compose-ci.yaml

      - name: Unit tests
        run: go test -race -count 100 -timeout=5m ./...
        working-directory: .
